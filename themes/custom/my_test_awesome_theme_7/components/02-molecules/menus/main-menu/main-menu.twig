{% include "@molecules/menus/_menu.twig" with {
  menu_class: "main-menu-bs",
  items: menu_items,
  list__addition_classes: [
    'navbar-nav',
    'ms-auto',
  ],
  list__attributes: list__attributes|default({})|merge(test('main-menu')),
  data_analytics: data_analytics|default({})|merge({
    group: 'main-menu',
  }),
} %}

